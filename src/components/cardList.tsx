import ProjectCard from "./card";
import type { cardParts } from "./card";

interface gitRepo {
    name: string;
    description: string;
}

export default function CardList(repoList: []) {
    
    //const cards = repoList.map(gitRepo => {gitRepo.name,})

   // return ();
}